{
  "name": "ad-blocking-proxy-server",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["proxy", "ad-blocker", "express", "cheerio"],
  "author": "",
  "license": "ISC",
  "description": "Ad-blocking proxy server for web content",
  "dependencies": {
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "express": "^5.1.0"
  }
}
